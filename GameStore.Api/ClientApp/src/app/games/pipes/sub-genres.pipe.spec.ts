import { SubGenresPipe } from './sub-genres.pipe';

describe('SubGenresPipe', () => {
  it('create an instance', () => {
    const pipe = new SubGenresPipe();
    expect(pipe).toBeTruthy();
  });
});
