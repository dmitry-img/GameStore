import { ExceptRoleDirective } from './except-role.directive';

describe('ExceptRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new ExceptRoleDirective();
    expect(directive).toBeTruthy();
  });
});
